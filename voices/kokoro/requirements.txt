# Magister Kokoro voice service — Slice 6C
#
# Install: ./start.sh (creates .venv and pip-installs everything below).
#
# System dependency NOT in pip:
#   espeak-ng must be installed at the OS level (Kokoro G2P requires it).
#     Debian/Ubuntu: sudo apt install espeak-ng
#     macOS:         brew install espeak-ng
#     Fedora:        sudo dnf install espeak-ng

# Web framework + ASGI
fastapi>=0.110
uvicorn>=0.29

# Pydantic v2 (FastAPI dep, pinned to avoid v1 surprises)
pydantic>=2.5

# Kokoro 82M TTS engine (Apache-2.0)
kokoro>=0.9.4
misaki[en]>=0.9.4

# Audio encoding (libsndfile-backed) + numerics
soundfile>=0.12
numpy<2.0
